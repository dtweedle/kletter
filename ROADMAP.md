# Road map

## Stage 0 (In Development)

    - Builds working
    - Outputs an svg file
    - Can render a basic SVG canvas that is 200 x 200 pixels
    - Can take take a list of points and render them to the canvas

## Stage 1 (Future Development)

    - Can load take in the dimensions of an image and constrain an SVG overlay to the size of that image
    - Can scale up or scale down the size of the overlay to match different image scalings.

## Stage 2 (Future Development)

    - Can take in an image and render the image to an html element by using the source element and base64 encoding the image
    - Can render the overlay over that image
    - Can read the file from a file system or from a url

## Stage 3 (Future Development)

    - Can group segments of points together to form a path or route segment
    - Add individual images for each point to help differentiate between points

## Stage 4 (Future Development)

## Stage 5 (Future Development)

## Stage 6 (Future Development)
